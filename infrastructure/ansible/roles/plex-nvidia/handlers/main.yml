---
# Handlers for plex-nvidia role

- name: Restart Docker
  systemd:
    name: docker
    state: restarted

- name: Reboot system
  reboot:
    msg: "Rebooting to load Nvidia drivers"
    reboot_timeout: 600

- name: Restart Plex
  community.docker.docker_compose_v2:
    project_src: "{{ plex_compose_dir }}"
    state: restarted
