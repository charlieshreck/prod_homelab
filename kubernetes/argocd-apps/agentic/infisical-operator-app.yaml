apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: infisical-operator-agentic
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: default
  sources:
    # Helm chart for infisical-operator
    - repoURL: https://dl.cloudsmith.io/public/infisical/helm-charts/helm/charts/
      chart: secrets-operator
      targetRevision: 0.10.2
      helm:
        releaseName: secrets-operator
    # Git repository for authentication credentials
    - repoURL: https://github.com/charlieshreck/agentic_lab.git
      targetRevision: main
      path: kubernetes/platform/infisical/resources
  destination:
    server: https://10.20.0.40:6443
    namespace: infisical-operator-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
