apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cleanuparr
  namespace: media
  annotations:
    traefik.ingress.kubernetes.io/router.tls: "true"
    gethomepage.dev/enabled: "true"
    gethomepage.dev/name: "Cleanuparr"
    gethomepage.dev/description: "Media cleanup automation"
    gethomepage.dev/group: "Media"
    gethomepage.dev/icon: "si-sonarr"
    gethomepage.dev/pod-selector: "app=cleanuparr"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - cleanuparr.kernow.io
      secretName: kernow-io-tls
  rules:
    - host: cleanuparr.kernow.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cleanuparr
                port:
                  number: 11011
